export enum EndpointEnum {
    // --------------- auth endpoint ----------------
    login = "/login",
    registration = "/register",

    // --------------- user endpoint ----------------
    users = "/users"
}